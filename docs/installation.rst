============
Installation
============

At the command line::

    $ easy_install datacleaner

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv datacleaner
    $ pip install datacleaner
